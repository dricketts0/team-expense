<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">Team Expense!</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/">File Expenses</a>
      </li>
      {{#if isAdmin}}
      <li class="nav-item">
        <a class="nav-link" href="/users">Users</a>
      </li>
      {{/if}}
      {{#if isAdmin}} 
      <li class="nav-item">
        <a class="nav-link" href="/teams">Teams</a>
      </li>
      {{/if}}
      {{#if isSupervisor}}
      <li class="nav-item">
        <a class="nav-link" href="/teamProfile/{{user.teamId}}">Team Profile</a>
      </li>
      {{/if}}
      <li class="nav-item">
        <a class="nav-link" href="/logout">Logout</a>
      </li>
    </ul>
  </div>
</nav>
<br><br>
<div class="container-fluid">
  <h2> {{#if team}} Edit Team {{teamCode}} - {{team.teamName}} {{else}} Let's Add a Team!{{/if}}</h2>
  <br><br>
  <form action="/submitTeam"  method="POST">
    <div class="form-group col-sm-2" >
        <label for="codeInput" class="col-form-label">Code</label>
    </div>
    <div class="form-group col-sm-2" align="left">
        <input type="text" class="form-control" id="codeInput" maxlength="4" value="{{team.teamCode}}" name="teamCode"/>
    </div>
     <div class="form-group col-sm-2" >
        <label for="nameInput" class="col-form-label">Name</label>
    </div>
    <div class="form-group col-sm-2" align="left">
        <input type="text" class="form-control"id="nameInput" value="{{team.teamName}}" name="teamName"/>
    </div>
     <div class="form-group col-sm-2" >
        <label for="budgetInput" class="col-form-label">Budget</label>
    </div>
    <div class="form-group col-sm-2" align="left">
        <input type="number" class="form-control"id="budgetInput"placeholder="0.00" value="{{team.budget}}"step="0.01" name="budget"/>
    </div>
    <br><br>
    <input type="hidden" name="id" value="{{team.id}}"/>
    <button type="submit">Submit</button>
       
  </form>

</div>
<br><br>